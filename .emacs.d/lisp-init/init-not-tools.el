;;; init-not-tools.el --- Non tool things -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(use-package fireplace
  :commands (fireplace))

(use-package snow
  :commands (snow))

;;; init-not-tools.el ends here
